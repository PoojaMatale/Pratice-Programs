using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Security;
using System.Text;
using System.Threading.Tasks;

namespace Pratice_Programs.Loop
{
    /**internal class Amstrongornot
    {
        static void Main(string[] args)
        {
            int rem = 0;
            int sum = 0;
            Console.WriteLine("enter the number");
            int num = Convert.ToInt32(Console.ReadLine());
            int temp = num;
            while (num != 0)
            {

                rem = num % 10;
                sum = sum + (rem * rem * rem);
                num = num / 10;
            }   

                if(temp==sum)
                {
                    Console.WriteLine("it is amstrong number");
                }
                else
                {
                    Console.WriteLine("it is not amstrong number");
                }

            
        }**/
}
